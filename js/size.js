// Размер области без учета padding и прокрутки 
// const height = box.clientHeight;

// Размер области с учетом  учетом padding и прокрукти.
// const height = box.offsetHeght;

// Выстота с учетом той, что скрыта за область прокрутки.
// const height = box.scrollHeight;

// Получить положение элемента
// box.getBoundingClientRect(); 

// Получение Computed (идут из CSS. Не путать с inline стилями) стилей. Только для чтения.
// const style = windows.getComputedStyle(box);
